//WAP for area of circle
//Should use a fixed variable pi to make statement simpler
#include<stdio.h>
int main(){
    int side;//assigning variable side int data type
    printf("side is:");
    scanf("%d", &side);//to enter value of side of cube
    printf("volume of cube is: %d",side*side*side);
    return 0;
}