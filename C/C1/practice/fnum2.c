#include<stdio.h>
/*
way to input value of variables in the code
use %d for int
use %f for float
use %c for characters
*/
int main(){
    int age=25;
    printf("age is %d \n",age);
    float pi=3.14;
    printf("pi is %f  \n",pi);
    char star='*';
    printf("star is %c",star);

    return 0;
}